// eslint-disable-next-line no-shadow
export enum accessLevel {
  none = 0x0000,
  default = 0x1000,
  admin = 0x2000,
}

// eslint-disable-next-line no-shadow
export enum hogwartsHouse {
  Slytherin = 0x0001,
  Gryffindor,
  Hufflepuff,
  Ravenclaw,
}
